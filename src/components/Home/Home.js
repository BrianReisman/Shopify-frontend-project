import React from 'react';
import { ViewHeader, ViewRoot } from "../../GlobalComponents";

const Home = (props) => {
  return(
    <ViewRoot>
      <ViewHeader>

      test from Home.js
      </ViewHeader>
    </ViewRoot>
)
};

export default Home;
